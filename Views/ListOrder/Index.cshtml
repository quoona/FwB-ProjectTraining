@model IEnumerable<FwB.Models.ListOrder>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ListOrderId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ListOrderId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ListOrderId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
