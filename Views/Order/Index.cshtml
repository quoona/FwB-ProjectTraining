@model IEnumerable<FwB.Models.ItemByIdOrder>


@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<div class="card-columns">
            @foreach (var item in Model)
{
                <div class=card>
                    <h3 class="card-header">@item.Items.ItemName</h3>
                    <div class="card-body" style="height: 150px;">
                        <div>Item Order Id: @item.Orders.OrderItemId</div>
                        <div> Order Id: @item.Orders.OrderId</div>
                        <div>Item Description: @item.Items.Description</div>
                    </div>
                    <div class="card-footer">
                        <div style="display: flex; justify-content: space-around;">
                            <span class="text-muted">Quantity: @item.Orders.Quantity</span>
                            @* <a asp-route="addcart" asp-route-itemid="@item.ItemId"
                                class="btn btn-primary btn-sm float-right">Chọn</a> *@
                            <a asp-action="Edit" asp-route-id="@item.Orders.OrderId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Orders.OrderId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Orders.OrderId">Delete</a>
                        </div>
                    </div>
                </div>
            }

        </div>